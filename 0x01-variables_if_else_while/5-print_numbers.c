#include <stdio.h>

/**
 * main -> program that prints the alphabet in lowercase 
 * Return : always 0
 */

int main(void)                                                                                                                                
{             
	int n;
	for (n = 0; n < 10; n++);
	{
		printf("%d\n", n);
	}

	return (0);
}
